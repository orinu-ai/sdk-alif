/*
 * SPDX-FileCopyrightText: Copyright 2026, orinu-ai
 * SPDX-License-Identifier: Apache-2.0
 */
/*********************    Autogenerated file. DO NOT EDIT *******************
 * orinu eyeCAM 30 keywords + _silence_ + _unknown_ = 32 classes
 * 순서는 학습 모델 출력 인덱스와 정확히 일치해야 함 (config.py LABELS)
 ***************************************************************************/
#include "BufAttributes.hpp"
#include <vector>
#include <string>
static const char *labelsVec[] LABELS_ATTRIBUTE = {
        "orinu", "eyecam", "capture", "photo",
        "record", "zoom_in", "zoom_out", "read",
        "describe", "detect", "identify", "translate",
        "summary", "stop", "pause", "resume",
        "cancel", "repeat", "louder", "softer",
        "mute", "emergency", "call", "help",
        "yes", "no", "setting", "home",
        "sleep", "status", "_silence_", "_unknown_",
};
bool GetLabelsVector(std::vector<std::string> &labels)
{
        constexpr size_t labelsSz = 32;
        labels.clear();
        if (!labelsSz) {
                return false;
        }
        labels.reserve(labelsSz);
        for (size_t i = 0; i < labelsSz; ++i) {
                labels.emplace_back(labelsVec[i]);
        }
        return true;
}
